# TODO — Things I Never Said (backend + infinite scroll)

- [x] Inspect existing backend confessions controller/service/entity and frontend read/write/globals files.
- [ ] Verify backend DB connection settings and add/confirm DB credentials wiring (env vars).
- [ ] Ensure frontend read page uses backend paginated API, shimmer cards, infinite scroll, debounced search, and full confession popup.
- [x] Ensure `shimmer-card.js` exists and matches styling hooks in `globals.css`.

- [x] Start backend and test API endpoints via curl.

- [x] Build frontend and run Next dev to verify infinite scroll + modal behavior.

- [x] Resolve any runtime/compile errors.


